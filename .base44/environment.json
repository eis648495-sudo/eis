{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_SUPABASE_URL",
      "description": "Your Supabase project URL (e.g. https://xxxxxxxx.supabase.co) from the Supabase dashboard > Project Settings > API > Project URL.",
      "requiredAtBoot": true
    },
    {
      "name": "VITE_SUPABASE_ANON_KEY",
      "description": "Your Supabase project anon/public key from the Supabase dashboard > Project Settings > API > Project API keys (anon public).",
      "requiredAtBoot": true
    }
  ]
}
